import ApplicationTracker from '@/components/resume-application/ApplicationTracker';

export const dynamic = 'force-dynamic';

export default function ApplicationsPage() {
  return <ApplicationTracker />;
}
